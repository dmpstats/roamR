---
title: "test-article"
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

Testing this as not sure is working! Something else to say.

```{r setup}
library(roamR)
```

And here goes a plot.

```{r}

plot(1:1000, rnorm(1000), type = "l")

```
