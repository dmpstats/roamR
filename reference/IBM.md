# Create a `<IBM>` object

Helper function to construct instances of
\<[`IBM`](https://dmpstats.github.io/roamR/reference/IBM-class.md)\>
objects

## Usage

``` r
IBM(
  agents = list(),
  species = new("Species"),
  drivers = list(),
  model_config = ModelConfig()
)
```
