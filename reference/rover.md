# A mock [Species](https://dmpstats.github.io/roamR/reference/Species-class.md) object

A `<Species>` object for a fictitious species with mocked up data,
solely for testing and demonstration purposes.

## Usage

``` r
rover
```

## Format

An object of class `Species` of length 1.

## See also

Helper function
[`Species()`](https://dmpstats.github.io/roamR/reference/Species.md) to
define `<Species>` objects
