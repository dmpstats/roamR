# Compute vector field of a stars raster

Calculate the vector fields (rasters aspect and slope) of a stars object
for a single attribute, for multi-dimensions

## Usage

``` r
compute_vector_fields(strs, unit = "radians")
```
